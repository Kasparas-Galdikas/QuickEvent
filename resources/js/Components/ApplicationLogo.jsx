export default function ApplicationLogo(props) {
    return (
        <img
            {...props}
            src="/images/logo1.png" // Path to your custom logo in the public directory
            alt="Logo"
        />
    );
}
